name: $name

# conda_env: conda.yaml

entry_points:
  main:
    parameters:
      base_dir: { type: string, default: "" } 
      scenario: { type: string, default: "" } 
    command: python src/run.py  --base_dir {base_dir} --scenario {scenario} 
  